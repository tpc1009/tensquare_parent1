server:
  port: 9001
spring:
  application:
    name: tensquare-base
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/tensquare_base?serverTimezone=UTC
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true

